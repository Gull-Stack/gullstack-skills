---
name: site-builder
description: Build premium GullStack client websites to the Tier-S standard — 11ty + Nunjucks, Editorial Light, AEO schema, Vercel deploy.
---

# site-builder (pointer)

The canonical skill lives at the repo root. Do not duplicate its content — read it:

1. `site-builder/SKILL.md` — the full skill.
2. `ux-ui/SKILL.md` — the design law this wraps; name the client genre before any layout decision.
3. `DESIGN.md` — the door.

All paths are relative to the `gullstack-skills` repo root.

⛔ This file is a POINTER. If you find yourself editing design law here, you are
editing the wrong file — the canon is `site-builder/SKILL.md`. Two copies is how one
document became three versions and two audits reviewed different documents on
the same day (2026-08-08). `./check-drift.sh` fails the build if this ever
carries content again.
