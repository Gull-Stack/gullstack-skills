---
name: app-design
description: Product UI/UX for GullStack apps — the shell law, five page species, theming, affordance and consequence.
---

# app-design (pointer)

The canonical skill lives at the repo root. Do not duplicate its content — read it:

1. `app-design/SKILL.md` — the full skill. Read §Escalation routes FIRST: a11y and charts are MANDATORY hand-offs, not optional.
2. `ux-ui/SKILL.md` — the OTHER design skill. Marketing sites, not product UI. Pick by surface, never by habit.
3. `DESIGN.md` — the door: which skill governs which surface, and what is law vs evidence.

All paths are relative to the `gullstack-skills` repo root.

⛔ This file is a POINTER. If you find yourself editing design law here, you are
editing the wrong file — the canon is `app-design/SKILL.md`. Two copies is how one
document became three versions and two audits reviewed different documents on
the same day (2026-08-08). `./check-drift.sh` fails the build if this ever
carries content again.
