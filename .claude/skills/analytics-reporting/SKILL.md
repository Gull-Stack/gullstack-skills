---
name: analytics-reporting
description: Analytics & client reporting for GullStack — GA4 hygiene (Consent Mode v2, key-event taxonomy, April 2026 attribution restructure), UTM discipline, register-side ground truth, ROI math (MER/cost-per-lead), and the monthly client report that proves the retainer. Use when setting up measurement, reading campaign data, or producing any client-facing performance report.
---

# Analytics & reporting (pointer)

The canonical skill lives at the repo root. Do not duplicate its content — read it:

1. `analytics-reporting/SKILL.md` — the full skill. Facts marked **[load-bearing]**
   put wrong numbers in front of clients if wrong; check the client's GA4 attribution
   settings before trusting any channel table.
2. `google-ads/SKILL.md` + `meta-ads/SKILL.md` — the campaigns this measures
3. `bryce-method.md` — gate #1 wires the tracking this skill reads

All paths are relative to the `gullstack-skills` repo root.
