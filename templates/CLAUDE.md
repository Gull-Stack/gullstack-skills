<!-- TODO(fill): Project memory. Keep the finished file under 500 tokens —
     it loads on EVERY Claude session in this repo. Only what Claude needs on
     every single task belongs here; everything situational belongs in
     .claude/skills/repo-conventions/SKILL.md.
     See $GULLSTACK_HOME/examples/EXAMPLE-CLAUDE.md for what "done" looks
     like. doctor.sh fails while any TODO(fill) marker remains. -->

# <!-- TODO(fill): project name --> 

<!-- TODO(fill): one sentence on what this repo is and who uses it. -->

## Commands

<!-- TODO(fill): exact commands — dev, test (all + single file), lint,
     typecheck, build. Copy from package.json/Makefile, don't guess. -->

## Hard rules

<!-- TODO(fill): 3–7 bullets MAX. Only the rules that must never be broken
     on any task. If it only matters when adding certain kinds of code, it
     goes in repo-conventions instead. -->

## Where things live

<!-- TODO(fill): 3–5 bullets mapping domains to directories. -->
