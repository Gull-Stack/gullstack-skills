node_modules
.next
out
.env
.env.local
# 🔴 .env.example is DOCUMENTATION and must stay tracked. It was silently
# dropped once already, by a parent repo whose .gitignore had a blanket `.env*`
# — leaving the README telling people to copy a file that did not exist. The
# negation makes that impossible to repeat here.
!.env.example
*.tsbuildinfo
next-env.d.ts
.vercel
